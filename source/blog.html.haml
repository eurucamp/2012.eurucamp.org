---
title: Blog
body_class: blog
---
%article
  = wrapped :header do
    %h2 Blog

  = wrapped :section, :class => 'articles' do
    - blog.articles[0...20].each do |article|
      %time= article.date.strftime('%e %b %Y')
      %h3= link_to article.title, article.url_without_extension
      %p= article.summary
      %p= link_to 'read more', article.url_without_extension
      %p= "by #{article.data.author}"
