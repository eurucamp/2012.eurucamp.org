---
body_class: index
---

%article
  %header
    %h2
      %strong 17 - 19 August 2012
      %span Berlin

  /
    %section.intro
      = wrapped :p do
        One or two sentences about eurucamp. A little bit about the history, maybe a mood setter.

    = wrapped :section, :class => 'highlights' do
      %h3
        What to expect
      %ul
        %li.venue
          %img(src="/images/content/highlights/venue.png" alt="Awesome location!")
          A sentence about the awesome location and the great hotel.
        %li.content
          %img(src="/images/content/highlights/content.png" alt="Best speakers!")
          A sentence about jRuby, Rails Girls, great speakers or something.
        %li.spare-time
          %img(src="/images/content/highlights/spare_time.png" alt="Best BBQ!")
          A sentence about the lighter side of things, BBQ, siesta.

  = wrapped :section, :class => 'twitter-feed' do
    %h3 Lately on Twitter
    .tweets

  %section.sponsors
    = wrapped :header do
      %h3 Our sponsors

    = wrapped :div, :class => 'kastanien' do
      %h4 Kastanien
      %ul
        - { 'Edenspiekermann_' => 'http://edenspiekermann.com/',
            'Railslove'        => 'http://railslove.com/',
            'Scalarium'        => 'http://www.scalarium.com/' }.each do |name, url|
          = wrapped :li do
            = link_to url do
              %img(src="/images/content/sponsors/#{name.underscore}.png" alt="Sponsor: #{name}")

    = wrapped :div, :class => 'linden' do
      %h4 Linden
      %ul
        - { 'Edenspiekermann_' => 'http://edenspiekermann.com/',
            'Railslove'        => 'http://railslove.com/',
            'Scalarium'        => 'http://www.scalarium.com/' }.each do |name, url|
          = wrapped :li do
            = link_to url do
              %img(src="/images/content/sponsors/#{name.underscore}.png" alt="Sponsor: #{name}")

    = wrapped :div, :class => 'birken' do
      %h4 Birken
      %ul
        - { 'Edenspiekermann_' => 'http://edenspiekermann.com/',
            'Railslove'        => 'http://railslove.com/',
            'Scalarium'        => 'http://www.scalarium.com/' }.each do |name, url|
          = wrapped :li do
            = link_to url do
              %img(src="/images/content/sponsors/#{name.underscore}.png" alt="Sponsor: #{name}")

